version: '3.8'

services:
  postgres:
    image: postgres
    ports:
      - "5343:5343"

    environment:
      POSTGRES_USER: postgres
      POSTGRES_DB: review
      POSTGRES_PASSWORD: password

  redis:
    image: redis
    ports:
      - "6379:6379"

